


const initialState = {
  totalUserScore: null,
  questionAccuracy: [],
  loading: false,
  error: false
};

export const statsReducer = (state = initialState, action) => {
  switch (action.type) {


    default: return state;

  }
}

export default statsReducer;